---
status: accepted
---

# 第一阶段通过按需 SSH 隧道访问结果页

目标服务器当前没有 Tailscale Serve 配置，操作者也不应擅自改变服务器或 tailnet 的持久
网络配置，因此第一阶段由 Windows CMD 建立两端仅绑定回环地址的 SSH 隧道，并在连接期间
启动只读结果服务；实时试验时控制与浏览分别占用两个可见 CMD 窗口，不创建隐藏进程。这样
无需管理员级网络变更且浏览服务不会失控常驻，代价是每次浏览都要保持一个独立 SSH 会话，
其他命名结果查看者要等管理员批准后再通过 Tailscale Serve 接入。
