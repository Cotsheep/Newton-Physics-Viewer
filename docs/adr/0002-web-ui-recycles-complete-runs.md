---
status: accepted
---

# Web UI 只回收完整试验运行

第一阶段结果浏览页保持静态只读；后期需要网页清理能力时，由 Tailscale Serve 将已验证的
用户身份转交给只监听本机的结果管理服务。访问默认拒绝：命名的 `result-viewers` 只能
查看，范围更小的 `result-maintainers` 才能回收和恢复完整、已停止的试验运行。Web UI
不能删除单个视频或永久清理；永久清理继续使用受限 SSH 命令，以保留结果完整性并限制
网页写权限的影响范围。
