---
status: accepted
---

# 按试验模板判定资产就绪状态

资产验收入库不要求一个版本同时满足所有试验模板；通用完整性检查通过且至少一个已批准
模板就绪时即可入库，并为每个模板分别记录就绪状态与原因。这样少量部分就绪资产仍可参加
适用试验，代价是控制端必须禁用不适用组合、远程端必须再次校验，且模板或检查器更新后需要
按需使用 CPU 重新评估已有版本，不能把旧检查器产生的状态永久沿用。
