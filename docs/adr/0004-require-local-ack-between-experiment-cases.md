---
status: accepted
---

# 每个工况结束后要求本地续行确认

当前 Docker 服务器没有调度器或 systemd，用户也不希望试验在本地电脑失联后自主展开。
远程编排器允许已经开始的一个有限工况完整收尾并保存结果，但只有收到本地控制程序针对该
工况的明确续行确认后才启动下一个工况；确认超时则把运行标记为 `interrupted` 并退出。
该边界用一次工况级握手替代持续心跳、总墙钟硬上限和 tmux 后台运行。
