---
status: accepted
---

# 正式 GPU 试验要求管理员控制的授权策略

目标容器当前暴露多块 GPU，而个人账号可写的 UUID 列表只能防止误操作，不能形成真实权限
边界。因此正式试验只接受容器设备隔离或管理员提供的只读 GPU 授权策略；缺少该边界时仍可
做诊断、资产验收和 CPU 冒烟，但不能运行正式 GPU 批次。首版还默认要求获授权设备独占
空闲，竞争出现时不触碰其他进程而是在工况边界停止，以管理员配合和较低设备利用率为代价
避免把个人配置或空闲推断误称为服务器授权。
