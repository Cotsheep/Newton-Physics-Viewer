---
status: accepted
---

# 多模板请求拆分为独立试验运行

一次 CMD 确认创建一个试验批次，但批次中的每个任务模板分别产生独立、串行的试验运行，
共享确定的资产版本和运行配置。这样 Web UI 分类、运行状态、重跑和结果回收都保持模板
边界，模板特有失败也可以隔离后继续下一运行；公共环境或完整性失败仍停止整个批次。代价是
需要额外的批次元数据和错误作用域分类来关联并安全编排一次请求产生的多个运行。
