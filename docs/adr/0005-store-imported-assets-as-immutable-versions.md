---
status: accepted
---

# 将导入资产保存为受引用保护的不可变版本

WinSCP 上传的资产只有通过验收入库后才进入正式资产库，并按完整依赖内容哈希保存为不可
覆盖的版本；验收成功时在同一文件系统内原子移动而不保留第二份，使运行请求和试验结果
能够准确复现实际输入。系统不自动删除资产版本；仍被正常结果或结果回收项引用的版本禁止
回收，只有无引用版本才能通过受限 CMD 操作先移入可恢复区域，再由独立的明确操作永久
清理；资产修改只能在本地来源中完成并以完整新包重新验收入库，以存储开销和重传成本换取
输入可追溯性、版本不可变性和误删保护。
