# Newton 资产质量检查

本上下文描述资产在 Newton 中被查看、运行和检查时使用的项目语言。

## Language

**试验运行**:
对一个指定资产按一个批准的任务模板和运行配置进行无交互物理检查，并保留观察与复现所需
结果的一次执行。
_Avoid_: 远程 Viewer、远程桌面、交互式会话、通用仿真任务

**试验批次**:
操作者一次确认提交的一组串行试验运行；批次固定资产版本和运行配置，每个所选任务模板产生
一个独立试验运行。
_Avoid_: 单个试验运行、并行 GPU 队列、合并结果包、合并录像

**任务模板**:
服务器批准的一类任务定义，限定可接受的输入、运行行为和预期结果。
_Avoid_: Shell 模板、命令字符串

**斜坡摩擦试验**:
在一组统一坡度和参考条件下观察资产滑动、滚动、翻倒与停止表现的物理检查。
_Avoid_: 摩擦系数测量、任意材质对试验

**预测滑动临界坡度**:
由资产标注的滑动摩擦系数按理想斜面关系推导出的开始滑动参考角度。
_Avoid_: 实测临界坡度、真实临界坡度、资产临界坡度

**摔落试验**:
从相对资产尺寸定义的多个高度释放资产并观察回弹、穿透、翻滚与稳定过程的物理检查。
_Avoid_: 材料常数测量、单次自由落体

**资产特征尺寸**:
资产在初始姿态下全部碰撞几何轴对齐包围盒的最大边长，不包含试验环境几何。
_Avoid_: 视觉包围盒高度、固定一米尺度

**试验资产**:
已经组合几何、刚体信息和待检查物理参数，可作为试验唯一输入的资产。
_Avoid_: 未组合的几何层、运行时临时拼接的 USD 层

**资产待导入区**:
个人账号中用于通过 WinSCP 明确接收完整资产包的暂存区域；其中内容尚未成为可运行的
试验资产。
_Avoid_: 正式资产库、Git 仓库、自动同步目录、可直接运行的任意路径

**服务器数据根目录**:
个人账号在服务器获准使用并经本地控制向导确认的持久化目录；资产待导入区、正式资产库、
运行结果、回收站和静态 Web 文件使用其固定子目录。
_Avoid_: 源码工作树、用户主目录本身、任意系统路径、多个互不关联的存储根

**Viewer 资产源目录**:
本地桌面 Viewer 用于发现和读取原始 URDF、USD、GLB 数据集的目录；Viewer 不在其中创建
试验运行、结果视频或 Web 索引。
_Avoid_: 试验数据目录、资产待导入区、正式资产库

**试验数据目录**:
本地开发验证使用的受管理数据根目录；它与 Viewer 资产源目录和源码工作树相互独立，并按
与服务器数据根目录相同的固定布局保存待导入资产、不可变资产版本和非正式运行结果。
_Avoid_: Viewer 资产源目录、源码工作树、用户主目录本身、临时输出文件夹

**统一控制入口**:
由 `uv run newton-test` 或 Windows `newton-test.cmd` 打开的本地持续中文菜单；它保存非敏感
个人设置并直接执行已开放操作，但不自动部署服务器，也不把未实现能力显示为可运行选项。
_Avoid_: Newton 物理引擎入口、远程守护服务、Web 任务提交页、自动安装器

**资产验收入库**:
对资产待导入区中的完整资产包执行依赖解析、物理预检和内容哈希，并在全部通过后将其作为
不可变资产版本加入正式资产库的操作。
_Avoid_: 上传资产、运行试验、登记资产、修改资产、任意文件移动

**资产验收报告**:
一次资产验收入库尝试产生的小型结构化记录，说明检查对象、已完成检查、错误或成功版本，
并使用相对路径描述资产内容。
_Avoid_: 资产副本、试验结果、服务器绝对路径、仅在终端显示的临时文本

**试验就绪状态**:
一个具体资产版本针对一个批准的任务模板是否满足执行前置条件的版本化检查结果，并在未
就绪时包含明确原因。
_Avoid_: 资产物理质量结论、运行成功状态、跨模板统一通过标记、跨检查器版本永久有效

**部分就绪**:
一个资产版本通过通用入库检查，且至少一个但并非全部批准任务模板处于就绪状态。
_Avoid_: 部分成功、试验执行失败、全部模板不可运行、资产部分合格

**资产身份**:
由资产待导入区中直接包含 `newton-mujoco.usda` 的资产包目录相对路径规范化而成、用于
跨多次试验归档同一逻辑资产的稳定标识。
_Avoid_: 文件名、内容哈希、显示名称、内部存储路径

**资产版本**:
由试验入口及其完整依赖内容的 SHA-256 确定、准确代表一次试验实际读取内容的不可变版本。
_Avoid_: 资产身份、修改时间、人工版本标签

**资产版本回收**:
将一个未被任何正常结果或结果回收项引用的不可变资产版本移入可恢复区域，并保持其内容
不变。
_Avoid_: 覆盖资产、自动过期、清理待导入文件、结果回收、立即永久删除

**工况**:
一次试验运行中由一个具体坡度或释放高度定义的独立仿真过程。
_Avoid_: 整次试验运行、合并录像章节

**工况续行确认**:
本地控制端在一个工况完整结束后发出的明确确认，允许远程编排器启动下一个工况。
_Avoid_: 持续心跳、SSH 在线推断、整次运行授权

**运行请求**:
操作者确认后用于发起一次试验批次的结构化说明，包含目标资产、试验种类、运行配置、
工况参数和获授权计算资源选择。
_Avoid_: 任意 shell 命令、未经确认的临时参数、服务器登录凭据

**运行配置**:
一组命名且经批准的仿真条件，完整限定一次资产运行所采用的求解器和数值设置。
_Avoid_: 任意参数组合、零散求解器参数

**本地冒烟运行**:
使用本地 Windows 或 WSL MuJoCo CPU 配置执行的短时开发验证，只证明代码与结果链路可运行，不产生
批准的资产物理试验结果。其 MuJoCo/Warp 物理解算不使用 CUDA；Windows 录像的系统 OpenGL
可能使用本机图形 GPU，Linux 录像则必须验证为 Mesa 软件 OpenGL。
_Avoid_: 正式试验运行、GPU 验收、资产物理结论、可比较基准

**GPU 集成冒烟运行**:
由 Determined experiment 分配恰好一个 GPU 后，在 trial 容器内用 Newton `SolverMuJoCo`、
MJWarp/CUDA 和 MuJoCo 原生接触执行的单个受限时中等高度摔落开发检查。它只验证 GPU 执行
与结构化结果链路，始终 `authoritative=false`，不由 Newton-Test 自行提交 experiment。
_Avoid_: 正式 GPU 试验、宿主机 GPU 编号选择、多高度批次、坡度批次、参数扫描、物理结论

**运行结果**:
一次 **试验运行** 留下的日志、运行中预览、结果录像、最终截图和运行清单。
_Avoid_: 仅指最终截图、仅指判定结果

**结果回收**:
将一个已经停止的完整试验运行从可见结果库移入可恢复区域，并保持其内部结果不变。
_Avoid_: 单独删除视频、自动过期、立即永久删除、任意路径删除

**结果浏览页**:
以资产为入口，只读组织、播放和下载试验状态与运行结果的本地浏览器界面。
后期可在经授权的身份下发起完整运行的结果回收与恢复，但不编辑运行内部产物。
_Avoid_: 任务提交页、远程控制台、结果编辑器、单文件删除器

**按需结果浏览会话**:
由 Windows CMD 控制程序建立、两端只绑定回环地址并随 SSH 连接退出的只读结果访问通道。
SSH 连接经 Tailscale 私有地址到达服务器，但不配置持久 Tailscale Serve。
_Avoid_: 公网 Web 服务、共享链接、常驻后台服务、远程桌面、试验控制连接

**服务器就绪检查**:
在不启动 Newton、CUDA 或正式试验的情况下，诊断服务器连接、存储、部署环境、编码器、
GPU 授权配置和结果浏览隧道是否满足当前阶段要求。
_Avoid_: 自动修复、GPU 压力测试、权限授予、永久授权、Tailscale 配置

**管理员确认清单**:
服务器就绪检查根据缺失的外部授权或策略生成、供操作者复制给服务器所有者确认的本地
文字，不会被程序自动发送。
_Avoid_: 自动权限申请、凭据文件、服务器配置、已获授权证明

**GPU 授权策略**:
由服务器管理员控制且当前个人账号不能修改的允许 GPU UUID、默认 GPU 和资源限制定义，
或等价的容器设备隔离。
_Avoid_: 用户可写允许列表、空闲 GPU 推断、口头授权记录、设备 0 默认值

**资产封面**:
首次发现试验资产时，根据其组合后的视觉外观以统一中性背景、固定三分之四视角自动生成的
16:9 静态图像，用于在结果浏览页中快速辨认资产。
_Avoid_: 人工必填封面、工况结果截图、物理质量判定、合并视频

**资产试验页**:
汇集一个试验资产的全部试验运行，并按试验种类组织工况视频的浏览页面；每类试验默认展开
最新运行，历史运行按时间倒序折叠且永不被新运行覆盖。
_Avoid_: 单次运行清单、资产编辑页、试验控制台

**工况视频条目**:
以仿真开始前的试验布置画面作为缩略图、代表一个工况结果录像的可视化播放入口；点击画面
在当前资产试验页内播放，标题或展开入口进入独立工况播放页。
_Avoid_: 纯文字下载链接、合并总览视频

**工况播放页**:
以单个工况录像为中心，同时展示该工况参数、资产版本、运行配置和结果状态的独立页面。
_Avoid_: 资产试验分类页、第二份结果录像、远程 Viewer

**结果查看者**:
被明确授权浏览、播放和下载运行结果，但不能回收、恢复或永久删除结果的人员。
_Avoid_: 所有 tailnet 成员、匿名访问者、结果维护者

**结果维护者**:
被明确授权在后期结果浏览页中回收和恢复完整试验运行的人员。
_Avoid_: 普通结果查看者、永久删除权限、集群管理员账号

**试验执行失败**:
试验因导入、后端兼容性或运行错误而没有产生有效物理观察的结果。
_Avoid_: 资产物理不合格、质量判定失败

**部分成功**:
一次包含多个工况的试验运行仅有部分工况产生有效运行结果。
_Avoid_: 成功、全部失败、资产部分合格

## Relationships

- **斜坡摩擦试验**和**摔落试验**分别由一个 **任务模板** 定义
- **斜坡摩擦试验**在公共固定坡度之外观察 **预测滑动临界坡度**附近的表现
- **斜坡摩擦试验**和**摔落试验**使用同一 **资产特征尺寸**定义相对场景尺度
- 一个 **服务器数据根目录**通过固定子目录容纳资产待导入、正式资产版本、资产回收项、
  运行结果、结果回收项和 Web 文件
- 一个 **Viewer 资产源目录**只供桌面查看；一个独立的 **试验数据目录**容纳本地冒烟链路
- **统一控制入口**保存并区分这两个目录，通过资产编号代替手写内部路径和完整版本哈希
- 一个 **任务模板** 可创建多个 **试验运行**
- 一个 **试验运行**只对应一个 **任务模板**
- 一个 **试验运行** 使用恰好一个 **运行配置**
- **本地冒烟运行**使用独立命名配置并显式标记为非正式，不能与远程正式试验直接比较
- **GPU 集成冒烟运行**使用独立命名配置，必须先通过 Determined trial/allocation/slot 与
  单个 NVIDIA GPU UUID 的防误用安全门，再只选择 trial 内的逻辑 `cuda:0`；环境变量不是
  不可伪造的认证，实际隔离由 Determined/NVIDIA runtime 提供；该运行不能调用或开放正式
  GPU profile
- 一个 **试验运行** 针对恰好一个 **试验资产**
- **资产待导入区**中的完整资产包只有通过 **资产验收入库**，才成为可选择的
  **试验资产**
- **资产验收入库**成功后把完整资产包原子移动为正式 **资产版本**；失败时保留待导入
  内容，重复版本不自动删除上传副本
- 每次 **资产验收入库**无论成功或失败都产生 **资产验收报告**；服务器保留结构化原件，
  CMD 显示中文摘要并在 Windows 保存副本
- 一个 **资产版本**对每个批准的 **任务模板**分别具有 **试验就绪状态**
- 通用检查通过且至少一个任务模板就绪的资产版本可以入库；并非全部模板就绪时标记为
  **部分就绪**，全部模板未就绪时拒绝入库
- **运行请求**只能选择对应任务模板处于就绪状态的资产版本，服务器在运行前再次检查
- CMD 向导先选择资产身份和版本，再显示可选任务模板；多模板运行请求要求所有所选模板
  均就绪，不能静默跳过未就绪模板
- **试验就绪状态**与检查器版本绑定；代码更新后旧状态需要按需 CPU 复查并追加报告，
  不全库自动扫描、不占用 GPU，也不改写历史结论
- 本地数据集中的资产包是唯一可编辑来源；验收入库失败后在本地修正并完整重传，不修改
  服务器上的待导入目录、正式资产版本或资产回收项
- 同一规范化资产包相对目录的新内容形成同一 **资产身份**下的新 **资产版本**；移动或
  重命名资产包目录形成新的资产身份
- 每个 **试验资产**由一个 **资产身份**归档，并可在不改变该身份的情况下产生多个
  **资产版本**
- 每次 **试验运行**只使用并记录一个确定的 **资产版本**
- 仍被正常运行结果或结果回收项引用的 **资产版本**不能执行 **资产版本回收**
- 一个经过本地确认并由服务器重新校验的 **运行请求**发起一个新的 **试验批次**
- 一个 **试验批次**固定一个资产版本和运行配置，并为每个所选任务模板创建一个独立、
  串行的 **试验运行**
- 同时选择首版两个模板时，试验批次默认先运行摔落、再运行斜坡；操作者可在 CMD 确认前
  调整，运行请求固定最终顺序
- 工况级错误允许当前试验运行继续，任务模板特有的运行级错误允许批次继续下一运行；资产
  完整性、GPU、驱动、磁盘或结果协议错误停止整个试验批次，未知错误按批次级处理
- `Ctrl+C`取消整个试验批次；控制连接失效允许当前工况收尾，但批次中断且不再启动后续
  工况或试验运行
- 新建 **运行请求**默认选择最新入库的 **资产版本**；保存后的运行请求固定准确版本，
  只有操作者显式更新并再次确认时才切换版本
- 一个 **试验运行** 包含一个或多个 **工况**
- 一个 **工况** 产生一个独立结果录像
- 除最后一个工况外，远程编排器只有收到本地控制端的 **工况续行确认**后才启动下一个工况
- 一个 **试验运行** 产生一组 **运行结果**
- 已经停止的 **试验运行**以完整运行作为 **结果回收**单位，不拆分或改写单个工况产物
- 第一阶段 **结果浏览页**完全只读；后期只有获授权身份可以从页面发起 **结果回收**和恢复，
  永久删除仍不属于浏览页能力
- 第一阶段 **结果浏览页**通过 **按需结果浏览会话**访问；该会话独立于试验控制连接，
  关闭后不影响试验或服务器磁盘上的结果
- 实时试验使用两个可见 CMD 窗口分别维持试验控制和 **按需结果浏览会话**；不创建隐藏
  浏览进程。只读服务不启动 GPU 物理仿真，本地浏览器播放视频时仍可能使用本机图形加速
- CMD 可执行 **服务器就绪检查**并显示缺项；检查不授予权限或运行仿真，正式批次启动时
  仍需重新执行关键检查
- 正式 GPU 试验只能使用 **GPU 授权策略**允许的设备；用户可写配置只能提供防误操作提示，
  不能授予或扩大 GPU 权限
- 第一阶段正式 GPU 试验要求获授权设备独占空闲；启动前发现其他负载就拒绝，运行中出现
  新竞争则当前工况只保留诊断并在边界停止批次，且绝不终止其他用户进程
- 正式批次启动前必须在管理员磁盘安全余量之外预留估算输出和临时编码空间；管理员未指定
  时默认保留 100 GiB，空间不足只停止运行而不自动删除任何资产或结果
- 服务器就绪检查可根据外部缺项生成 **管理员确认清单**；它只保存在 Windows 本地并由
  操作者决定是否发送
- **结果浏览页**首先以 **资产封面**展示一个或多个 **试验资产**
- **结果浏览页**只收录至少存在一次未回收试验运行的资产，不充当完整资产目录
- 点击 **资产封面**进入对应的 **资产试验页**
- **资产试验页**按试验种类组织该资产的 **试验运行**及其 **工况视频条目**
- 每类试验的最新 **试验运行**默认展开，历史运行按时间倒序折叠保留
- 点击 **工况视频条目**的画面在当前页直接播放该工况录像，也可进入对应的
  **工况播放页**
- **结果查看者**可以使用结果浏览页；只有 **结果维护者**可以在后期发起结果回收和恢复
- **试验执行失败**不会产生资产质量判定
- **部分成功**只描述工况执行完整性，不产生资产质量判定

## Example dialogue

> **Dev:** “用户关闭浏览器后，摔落过程还会继续吗？”
> **Domain expert:** “会。浏览器只观察这次 **试验运行**；它使用批准的 **运行配置** 完成 **摔落试验**并留下 **运行结果**。”

## Flagged ambiguities

- “远程查看”曾可能被理解为远程操纵桌面 Viewer；已明确为观察无交互的 **试验运行**。
- “本地查看试验结果”已明确为在浏览器观察运行中预览和完成后的结果录像等产物，不包含远程输入。
- “运行配置”曾可能指用户临时组合的一组参数；已明确为命名且经批准的 **运行配置**。
- “运行 `view.py`”已明确为复用 `asset_viewer` 的资产加载与渲染能力；自动试验的物理语义由自己的 **运行配置**确定，不以本地 Viewer 结果为基准。
- `newton-test` 已明确为 **统一控制入口**的命令名，不表示直接启动 Newton 物理引擎；无参数
  时打开持续菜单，底层命令继续保留供开发和自动化使用。
- “资产存储地址”已拆分为只读发现原始数据集的 **Viewer 资产源目录**和保存验收资产、运行
  结果的 **试验数据目录**；二者不能相同或互相包含。
- “资产临界坡度”已明确为根据 `physics:dynamicFriction` 推导的 **预测滑动临界坡度**，不是独立实测真值。
- “Web UI 管理结果”已明确为通过 **结果浏览页**只读组织、播放和下载，不包含创建、取消、删除或修改。
- “结果浏览页”已明确为资产优先的视频库：先显示 **资产封面**，再在 **资产试验页**按试验种类浏览并直接播放各 **工况视频条目**，而不是以运行清单和纯文字播放链接作为主要入口。
- **资产封面**已明确为从组合后的资产视觉外观自动生成的统一 16:9 图片；第一阶段不要求人工上传。
- “同一个资产”已明确由规范化相对路径形成的 **资产身份**判断；物理参数或依赖内容变化形成新的
  **资产版本**，但不会在首页产生重复资产入口。
- “删除视频”已明确为对完整、已停止试验运行执行可恢复的 **结果回收**；不单独删除
  `video.mp4`，也不从只读结果浏览页执行删除。
- 首页收录范围已明确为存在未回收试验运行的资产，包括运行中、失败和成功结果；从未运行的
  资产不展示，全部运行被回收后隐藏，恢复运行后重新出现。
- “后期在 Web UI 删除视频”已明确为经过 Tailscale 身份校验后回收或恢复完整、已停止的
  **试验运行**；不删除单个视频，且永久清理继续通过受限 SSH 命令执行。
- 结果访问已明确为默认拒绝：第一阶段通过个人 SSH 身份访问，实际查看者只有操作者本人；
  后期命名的 **结果维护者**是 **结果查看者**中的更小子集。公网、匿名用户和所有 tailnet
  成员均不默认获得访问权。
- 第一阶段不修改当前为空的 Tailscale Serve 配置；操作者通过 Windows CMD 建立两端仅
  监听回环地址的 **按需结果浏览会话**。Tailscale Serve 和命名查看者在管理员批准后作为
  后期升级。
- 第一阶段远程试验明确使用操作者的个人集群账号，不创建或共享 `newton-runner` 服务账号；
  代码先在操作者本地 WSL 中验证，再进入远程 NVIDIA GPU 验收。
- 远程进程托管方式不能凭“服务器”或“集群”名称推断：受管理集群遵守 Slurm/PBS 等既有
  调度器；只有管理员确认是独立 GPU 服务器后，才考虑个人 systemd user service 与 linger。
- 早期直接容器探针曾把目标环境理解为没有调度器的 Docker 实例；当前已确认 GPU 任务由
  Determined 0.38.1 调度并且必须作为 experiment 提交。Newton-Test 的 GPU integration
  smoke 只负责 trial 内前台执行，不实现提交器，也不使用后台守护方案。
- 正式本地控制端从 Windows CMD 运行并调用现有 `ssh.exe`；Windows 用户自己的 SSH 别名
  保存连接信息。仓库和 WSL 不复制私钥，也不保存真实服务器地址或用户名。
- 正式本地控制端采用交互式向导生成并保存可复用的 **运行请求**；操作者查看完整摘要并
  确认后才发送。服务器必须二次校验，Web UI 第一阶段不负责启动试验。
- 真实资产不进入 Git 仓库，也不由本地控制程序隐式上传；操作者通过 WinSCP 把完整资产包
  放入 **资产待导入区**，通过 **资产验收入库**成为不可覆盖的 **资产版本**后才能运行。
- 新建运行请求默认使用最新入库资产版本；历史版本需要主动选择，已保存运行请求固定完整
  版本哈希并且不能静默升级。
- CMD 控制程序提供“服务器存储设置”，只配置一个 **服务器数据根目录**；已有正式数据时
  普通设置入口不能改路径或自动迁移。
- CMD 控制程序提供 **服务器就绪检查**，在不启动 Newton/CUDA 或修改 Tailscale 的情况下
  报告连接、存储、部署、编码、GPU 授权和结果隧道状态。
- 已验收入库的资产版本不自动删除；未被任何正常结果或结果回收项引用的版本才允许通过
  受限 CMD 操作执行 **资产版本回收**，第一阶段 Web UI 不提供该操作。
- “验收入库”不再表示资产必须通过所有试验模板；已明确按模板记录 **试验就绪状态**，
  至少一个模板就绪即可入库并在必要时标记为 **部分就绪**。
- “同时运行摔落和斜坡”已明确为一个 **试验批次**包含两个独立 **试验运行**，而不是
  一个运行混合两种任务模板；二者可以独立重跑和结果回收。
- 多块 GPU “可见”不等于“获授权使用”；正式试验必须读取管理员控制且当前用户不可修改的
  **GPU 授权策略**。默认设备忙碌时拒绝启动，不自动改用空闲设备；只有操作者确认才能
  改选另一块获授权设备。
- GPU 空闲检查不构成授权；首版默认独占且不排队，管理员明确提供共享规则之前不能启用
  共享模式。
- Determined 的 `slots_per_trial: 1` 表示调度器分配一块 GPU。GPU integration smoke 要求
  `DET_EXPERIMENT_ID`、`DET_TRIAL_ID`、`DET_TASK_ID`、`DET_ALLOCATION_ID`、单项
  `DET_SLOT_IDS`（恰好一个非负整数，拒绝字符串和 bool）和 `DET_TASK_TYPE=TRIAL` 完整，且 `NVIDIA_VISIBLE_DEVICES` 恰好为一个
  GPU UUID。安全门通过后 Warp 还必须只看见一个设备并使用容器逻辑 `cuda:0`，不得接收
  宿主机 GPU 编号；任一元数据、可见性、设备数量或 CUDA 初始化检查失败均直接失败且不
  回退 CPU。
- GPU 共享底层函数要求由安全门签发的进程内强类型执行许可，不能仅凭 profile 的 CPU/GPU
  布尔字段执行 CUDA；正式 profile 即使持有许可仍拒绝。许可是防误用机制，不是认证。
- 模型创建前核对分配 UUID 与 Warp UUID。匹配记 matched；不匹配或非空非法格式直接失败；
  缺失只能记 unavailable/warning。manifest 和 case 同时保存两个 UUID 及验证状态。
- GPU integration smoke 的审计状态按阶段记录 scheduler metadata、可见 GPU 数、选定/初始
  化设备、模型设备和物理推进。每步 solver 返回并完成 CUDA 同步后才计数；首次成功前，`actual_compute_device` 保持
  `unknown` 且 `cuda_used=false`；完成固定 1000 步后才能标记
  `gpu_physics_completed=true`，失败结果保留最后成功阶段。
- manifest 保存工作树状态和源码 SHA-256。GPU 只接受干净且 commit 匹配的 checkout，或
  由 `experiment_runner.release` 从干净 commit 导出并验证清单的源码；导出不部署或提交。
- 当前 `check-readiness --json` 为只读部署检查；它不导入仿真栈、不创建 CUDA context、
  不写文件或自动修复。远程结果隧道必须先验证结构化报告，GPU 未开放不阻止只读浏览。
- `asset-readiness-v2` 要求所有绑定碰撞材质分别具备坡度所需摩擦字段；一个材质的完整
  属性不能代替其他材质缺失的属性。运行前重新检查，不改写历史验收报告。
- 本地 WSL 运行已明确为 **本地冒烟运行**：仍使用 MuJoCo 原生接触，但采用 CPU，仅验证
  代码和产物链路，所有结果显式标记为非正式。
- 本地冒烟已实现中档摔落和固定 25° 斜坡各一个两秒工况。当前视频为
  `640×360、50 FPS`，并保持 `0.001 s` 物理时间步；坡度结果只使用
  `moved`、`stayed_near_start` 或 `inconclusive` 描述开发观察，不构成正式摩擦结论。
- 当前固定 25° 本地坡度冒烟只接受恰好一个动态刚体；这是现有开发工况的实现限制，不是
  通用 `slope_friction` 就绪规则，也不是未来正式坡度试验的永久限制。
- 现有 Windows Conda Viewer 命令继续兼容；日常推荐入口改为锁定的 Python 3.12 `uv`
  环境。Windows、WSL 与远程服务器共享 `pyproject.toml` 和 `uv.lock`，运行配置只选择
  CPU/GPU，不改变依赖版本集合。
- **统一控制入口**的个人配置保存 Viewer 资产源目录、试验数据目录、SSH 别名、端口和是否
  自动打开浏览器，不保存密码、私钥、真实服务器地址或用户名；Windows 启动文件检测到
  缺少 `uv` 时只显示安装说明，不自动联网安装。
- GPU integration smoke 的 Python 3.12 隔离环境和锁定依赖必须在 experiment 激活、占用
  GPU 前由人工部署关口准备完成。trial 只检查并直接调用显式的 `NEWTON_TEST_PYTHON`，
  不执行普通 `uv run --frozen`、依赖解析、下载或安装；缺少预制环境时快速失败。本轮不
  建设自动安装器、离线依赖包或自定义镜像构建器。
- 默认模板的 Python 环境预装在镜像内；可替代为管理员认可的独立只读挂载。环境与数据
  挂载分开，绝对路径、系统、glibc、架构和 CUDA 兼容性须在 GPU 分配前确认。trial 检查
  运行依赖与锁文件的完整传递版本集合；PyYAML 仅属于开发测试组。
- 私有代码部署优先使用绑定单个仓库的只读 Deploy Key；服务器无法访问 GitHub 时，可通过
  现有 SSH 传输经校验的离线 Git bundle。两种方式都只运行干净、确定的 Git commit，不
  复制本地工作树，也不保留个人 GitHub 账号的长期登录凭据。
- 早期正式远程路线曾设计通过普通 SSH 人工触发代码部署；这不是当前 GPU integration
  smoke 的 trial 接受条件。当前入口由人工复核的 Determined 配置显式传入完整 commit 和
  预制 Python，不实现 webhook、自动部署或 experiment 提交器。
- 候选 commit 只有通过服务器冒烟测试后才成为已验收版本；部署失败时单工作树恢复上一个
  已验收 commit 和锁定环境，恢复失败则关闭正式试验入口。
- 同一资产同类试验的多次运行已明确为最新运行默认展开、历史运行按时间倒序折叠；新运行
  不覆盖旧运行。
- 工况录像播放已明确同时支持两种入口：视频卡片内直接播放，以及进入展示完整参数的独立
  **工况播放页**；二者读取同一个结果录像。
- **工况视频条目**的缩略图已明确为仿真开始前的试验布置画面；最终状态截图单独保留，
  不作为默认视频缩略图。
- 早期正式多工况路线不使用 30 分钟整次运行墙钟硬上限；该设计不适用于当前受限 GPU
  integration smoke。后者固定 1 秒物理时间、300 秒内部上限，并由 Determined 示例施加
  8 分钟硬超时。
- SSH 本身不理解工况边界；早期正式多工况路线中的本地控制程序与远程编排器使用
  **工况续行确认**协议。当前单工况 GPU integration smoke 不使用该协议，只在
  Determined trial 内以前台进程执行。
